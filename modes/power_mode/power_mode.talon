mode: user.power_mode
-
<phrase>: skip()
#What <phrase>: skip() does is that it recognizes every phrase that is not a command you have defined, and skips it. It reduces the number of false positives, but unfortunately adds false negativesma